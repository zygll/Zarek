
//月
var M = function (val) {
  if (val.length == 0 || val == undefined) {
    return;
  }
  if (val.length > 10) {
    return parseInt(val.substring(5, 7));
  }
  else {
    return val;
  }
}
module.exports.M = M;


//日
var D = function (val) {
  if (val.length == 0 || val == undefined) {
    return;
  }
  if (val.length > 10) {
    return parseInt(val.substring(8, 10));
  }
  else {
    return val;
  }
}
module.exports.D = D;




